public static Integer arraySum(List<Integer> numbers) {
    //code here
   Integer sum = 0;
   for(List<Integer> num : numbers){
            sum += num;
        }
}

/*Integer sum = 0;
    if(numbers != null) {
        for(List<Integer> num : numbers){
            sum += num;
            return num;
        }
    } else {
        return null;
    }
*/