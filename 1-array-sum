public static Integer arraySum(List<Integer> numbers) {
    //code here
   if(numbers != null) {
       numbers.sum();
       return numbers;
   }
}

/*Integer sum = 0;
    if(numbers != null) {
        for(List<Integer> num : numbers){
            sum += num;
            return num;
        }
    } else {
        return null;
    }
*/